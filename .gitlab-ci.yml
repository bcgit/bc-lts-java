stages:
  - test
  - testSSE
  - testAVX
  - testVAES
  - testVAESF



test-java-code:
  stage: test
  script:
    - "apply_overlay bc-java ./"
    - "sh gettestdata.sh"
    - . cienv-11.sh
    - "./gradlew clean withNative prov:build util:build pg:build pkix:build tls:build mail:build"

test-code-sse:
  stage: testSSE
  script:
    - "apply_overlay bc-java ./"
    - "sh gettestdata.sh"
    - "rm -rf /tmp/bcfipslibs"
    - "mkdir -p /tmp/bcfipslibs"
    - . cienv-11.sh
    - "./gradlew clean core:compileJava; \ cd native; \ ./build_linux.sh; \ cd .."
    - "./gradlew clean cleanNativeVariants withNative prov:build util:build pg:build pkix:build tls:build mail:build copyJars -x test"
    - "tmpLibDir=$(mktemp -d -t bc-jni-libs-XXXXXXXXXX)"
    - "export LD_LIBRARY_PATH=\"$LD_LIBRARY_PATH:$tmpLibDir\""
    - "java -Dorg.bouncycastle.native.cpu_variant=sse -cp jars/bcprov-lts8on-1.0.0-SNAPSHOT.jar org.bouncycastle.util.DumpInfo"
    - "./gradlew -Pskip.pqc.tests testSSE  -x test"


test-code-avx:
  stage: testAVX
  script:
    - "apply_overlay bc-java ./"
    - "sh gettestdata.sh"
    - "rm -rf /tmp/bcfipslibs"
    - "mkdir -p /tmp/bcfipslibs"
    - . cienv-11.sh
    - "./gradlew clean core:compileJava; \ cd native; \ ./build_linux.sh; \ cd .."
    - "./gradlew clean cleanNativeVariants withNative prov:build util:build pg:build pkix:build tls:build mail:build copyJars -x test"
    - "tmpLibDir=$(mktemp -d -t bc-jni-libs-XXXXXXXXXX)"
    - "export LD_LIBRARY_PATH=\"$LD_LIBRARY_PATH:$tmpLibDir\""
    - "java -Dorg.bouncycastle.native.cpu_variant=avx -cp jars/bcprov-lts8on-1.0.0-SNAPSHOT.jar org.bouncycastle.util.DumpInfo"
    - "./gradlew -Pskip.pqc.tests testAVX  -x test"

test-code-vaes:
  stage: testVAES
  script:
    - "apply_overlay bc-java ./"
    - "sh gettestdata.sh"
    - "rm -rf /tmp/bcfipslibs"
    - "mkdir -p /tmp/bcfipslibs"
    - . cienv-11.sh
    - "./gradlew clean core:compileJava; \ cd native; \ ./build_linux.sh; \ cd .."
    - "./gradlew clean cleanNativeVariants withNative prov:build util:build pg:build pkix:build tls:build mail:build copyJars -x test"
    - "tmpLibDir=$(mktemp -d -t bc-jni-libs-XXXXXXXXXX)"
    - "export LD_LIBRARY_PATH=\"$LD_LIBRARY_PATH:$tmpLibDir\""
    - "java -Dorg.bouncycastle.native.cpu_variant=vaes -cp jars/bcprov-lts8on-1.0.0-SNAPSHOT.jar org.bouncycastle.util.DumpInfo"
    - "./gradlew -Pskip.pqc.tests testVAES  -x test"

test-code-vaesf:
  stage: testVAESF
  script:
    - "apply_overlay bc-java ./"
    - "sh gettestdata.sh"
    - "rm -rf /tmp/bcfipslibs"
    - "mkdir -p /tmp/bcfipslibs"
    - . cienv-11.sh
    - "./gradlew clean core:compileJava; \ cd native; \ ./build_linux.sh; \ cd .."
    - "./gradlew clean cleanNativeVariants withNative prov:build util:build pg:build pkix:build tls:build mail:build copyJars -x test"
    - "tmpLibDir=$(mktemp -d -t bc-jni-libs-XXXXXXXXXX)"
    - "export LD_LIBRARY_PATH=\"$LD_LIBRARY_PATH:$tmpLibDir\""
    - "java -Dorg.bouncycastle.native.cpu_variant=vaesf -cp jars/bcprov-lts8on-1.0.0-SNAPSHOT.jar org.bouncycastle.util.DumpInfo"
    - "./gradlew -Pskip.pqc.tests testVAESF  -x test"

dependencies {
    implementation project(path: ':prov')
    implementation project(path: ':core')
}

processResources {
    from "resources"
}

test {

}

jar.archiveBaseName = "bc-benchmark-java"
version = "0.1"

compileJava {
    options.release = 8
}

compileTestJava {
    options.release = 8
}